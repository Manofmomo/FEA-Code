% Functions List (to be updated sequentially when changes are made)
% 1) Structure to define nodes and subsequently elements
node = struct('glob_coord',[0,0],'init_coord',[0,0], 'fin_coord', [0,0], 'stress', [0,0], 'shear_force', [0,0])

